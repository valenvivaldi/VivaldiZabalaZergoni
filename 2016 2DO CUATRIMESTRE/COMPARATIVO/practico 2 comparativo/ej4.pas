Program ej2;
var
	x:integer;
	y:integer;
	z:String;

 	
begin
	readln(x); // el error en tiempo de ejecucion saltaria cuando en vez de ingresar un numero por teclado le ingresamos un string por ejemplo
	
	writeln(x);
end.