-module(operaciones).
  -export([suma/2]).
  -export([resta/2]).
  -export([multiplicacion/2]).
  -export([division/2]).

suma(A,B) ->
A+B.

resta(C,D) ->
C-D.

multiplicacion(E,F) ->
E*F.

division(G,H) ->
G / H.